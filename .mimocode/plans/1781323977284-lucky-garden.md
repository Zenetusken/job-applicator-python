# Fix 6 Business Logic Gaps

## Files to modify
- `src/job_applicator/documents/resume_tailor.py` — Gaps 1, 2, 3, 4, 5
- `src/job_applicator/documents/cover_letter.py` — Gap 6
- `src/job_applicator/cli.py` — Gaps 5, 6
- `tests/unit/test_hallucination_guards.py` — Gaps 2, 3, 4
- `tests/unit/test_tailor_pipeline.py` — Gaps 1, 5
- `tests/unit/test_workflow.py` — Gap 6

## Execution order (dependency-safe)

### 1. Gap 4: parse_sections() over-matches ALL CAPS names
**Lines**: resume_tailor.py 34-88
**Fix**: Delete `SECTION_HEADER_RE`. Add `KNOWN_HEADERS` frozenset + `_COLON_HEADER_RE` + `_is_section_header()` helper. Update `parse_sections()` to use helper. Update `test_hallucination_guards.py` — change `test_all_caps_names_not_confused_with_headers` assertion to `assert "JOHN DOE" not in names`. Add tests for PROFESSIONAL SUMMARY, WORK HISTORY, TECHNICAL SKILLS, CORE COMPETENCIES.

### 2. Gap 2: Skill validation is substring-only
**Lines**: resume_tailor.py 768-774
**Fix**: Add `_skills_match()` helper using exact match + token containment (subset) + `SequenceMatcher` ratio >= 0.85. Replace substring check in `_validate_skills`. Add tests: "ai" NOT match "training", "Pythn" matches "python" via fuzzy, "API Design" matches "Design" via token containment.

### 3. Gap 3: Tool hallucination only checks job requirements
**Lines**: resume_tailor.py 787-883
**Fix**: Add Pass 2 after existing loop. For each key in `tool_replacements`: if tool in tailored text, NOT in original, NOT in job requirements → replace with generic (word-boundary regex). Add tests for Pass 2.

### 4. Gap 5: Double tone detection
**Lines**: resume_tailor.py 510-553, cli.py 770, 856
**Fix**: Add `tone_profile: ToneProfile | None = None` param to `tailor()`. Guard internal detection with `if tone_profile is None:`. In cli.py line 856, pass `tone_profile` to `tailor()`. Add tests: provided tone_profile skips detection, None triggers detection.

### 5. Gap 1: Stale match scores after refine()
**Lines**: resume_tailor.py 598-654
**Fix**: After hallucination guards, create synthetic `ResumeData(refined_text, ...)` and run `JobMatcher.match_resume_to_job()`. Use new scores in return. Update tests to verify scores change after refinement.

### 6. Gap 6: Cover letter refinement bypasses structured generation
**Lines**: cover_letter.py ~300, cli.py 507-550, 663
**Fix**: Add `REFINE_SYSTEM_PROMPT` and `refine()` method to `CoverLetterGenerator` (uses instructor + fallback, includes style/tone/job context). Rewrite `_refine_cover_letter` to delegate to `generator.refine()` with `resume_data`, `style`, `tone_section` params. Update call site at line 663 to pass new params. Update tests to mock `generator.refine()`.

## Verification
```bash
pytest tests/unit/ -v          # all tests pass
ruff check src/ tests/          # clean (except pre-existing ASYNC240)
ruff format --check src/ tests/ # all formatted
mypy src/job_applicator/ --ignore-missing-imports  # 0 errors
```
